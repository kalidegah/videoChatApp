const initState = {
    identity:'', // optional text
    isRoomHost:''
};

const reducer = (state= initState, action) => {
    switch (action.type) {
        default: 
          return state;
    }
}


export default reducer;
